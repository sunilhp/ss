// @flow
import React from 'react';
import {
  View,
  Text,
} from 'react-native-ui-lib';

type Props = {};

export default (props: Props) => (
  <View flex centerV centerH>
    <Text>NavigationType View</Text>
  </View>
);
